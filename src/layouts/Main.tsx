import { Button, Input } from '@mui/material';
import { FC } from 'react';

const Main: FC = () => {
  return (
    <div>
      <Input />
      <Button>Hadizha</Button>
      <Button>Adilet</Button>
      <Button>Aidin</Button>
      <p>fix bag first</p>
    </div>
  );
};

export default Main;
